# Known issues

| Status | Severity | Issue | Reproduction / evidence | Workaround / next owner |
|---|---:|---|---|---|
| BLOCKED | P0 | Branch protections/rulesets are unavailable for this private repository plan. | GitHub API returns HTTP 403 requiring Pro or public visibility. | Upgrade to GitHub Pro; do not make the private repository public merely for protections. |
| CONFIRMED | P1 | No remote tags or GitHub releases exist, while the package is `0.1.0-beta.1`. | GitHub refs/releases and `package.json`. | Release owner establishes provenance before tagging. |
| CONFIRMED | P1 | Cleanup CI is green but cannot yet be enforced. | PR #2 head `36191d2`: `CI / verify` succeeded; legacy `CI / quality` on prod failed. | After GitHub Pro, require exact context `CI / verify`; keep the legacy run only as historical evidence. |
| CONFIRMED | P2 | `npm audit` reports three high findings in dev/build dependencies. | 2026-08-08: `fast-uri@3.1.4` via electron-builder/ajv, `js-yaml@4.3.0` via electron-builder, `nanoid@3.3.16` via Vite/PostCSS. `npm audit --omit=dev` reports 0. | Separate lockfile-only dependency PR targeting `fast-uri>=3.1.5`, `js-yaml>=4.3.1`, `nanoid>=3.3.17`; no major update indicated. |
| PLANNED | P1 | Release-only provider UI matrix and 8–12 hour soak are not automated. | `docs/testing.md` and this cleanup's test evidence. | Release/UAT owner executes the closed profile before promotion. |
| BLOCKED | P1 | Anthropic is generic-web exploratory only; OpenRouter is not implemented. | Provider registry and release runbook review. | Do not mark either provider PASS without implementation and security review. |
| FIXED_LOCAL_LIVE | P1 | Gemini may add prose around a correct unique verification marker. | Structural parser accepts exactly the expected run/step marker inside provider prose and rejects absent, wrong or conflicting markers; 10 focused tests pass. The owner-approved Gemini attachment turn completed with the real provider session. | Retain structural/conflict tests; do not return to whole-response equality. |
| IMPLEMENTED_LOCALLY / LIVE_UAT_BLOCKED_EXTERNAL | P1 | Response-file retrieval is implemented locally, but final real ChatGPT/Gemini acceptance is incomplete. | 2026-08-25: decorative provider-card icons are rejected; Gemini confirmed the authenticated fetch-download path. The final Gemini check was interrupted by `ERR_NETWORK_CHANGED`; ChatGPT timed out and a read-only follow-up met a Cloudflare challenge. No blind retry or challenge bypass occurred. | Keep release/installer blocked. In a stable network and after the challenge disappears, run at most one controlled request per provider; never promote decorative images or `FAILED` records to result cards. |
| CONFIRMED | P2 | Windows/Electron does not consistently expose copied documents as a trusted clipboard `File` path. | Image bytes work through bounded IPC; non-image clipboard items attempt the trusted preload path and report a picker/drop fallback if unavailable. | Use attachment picker or drag-and-drop for documents. Do not expose filesystem paths to React. |
| FIXED_LOCAL | P1 | Простой запрос вроде «тест» мог породить до семи ходов обсуждения. | Консервативная semantic policy теперь ограничивает только явные test/presence/greeting/arithmetic prompts одним discussion-turn на провайдера и затем finalizer; focused orchestration regression проходит. | Подтвердить число видимых сообщений при owner-approved live UAT. Не расширять trivial-классификатор без fixtures. |
| BLOCKED_BY_AUTH | P1 | Компактный prompt lifecycle проверен локально, но количество видимых сообщений в реальных ChatGPT/Gemini чатах ещё не подтверждено. | Production orchestration tests доказывают один `sendMessage` на turn, persistent bootstrap/reuse/delta и отсутствие transcript replay. | Выполнить короткий authenticated provider UAT только после явного подтверждения владельца. |
| PARTIAL | P2 | Automatic desktop context rollover is not wired end-to-end. | Memory/checkpoint/rollover services and Orchestrator hooks are tested independently. | Add a provider UAT fixture proving checkpoint, new chat, URL update and decision preservation. |
| CONFIRMED | P2 | CLI V1 executes as the current host user and is not a VM-grade sandbox. | Broker and UI warning; security model. | Keep per-task approval, path/process/output limits and audit verification. Do not call this Secure Code Runtime. |
